import { FilterIcon } from '../icons';
import { ButtonIcon } from '../shared';

export const Filter = () => {
  return (
    <ButtonIcon variant='input'>
      <FilterIcon />
    </ButtonIcon>
  );
};
